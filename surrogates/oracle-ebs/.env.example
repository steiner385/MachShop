# Oracle EBS Surrogate Configuration

# Environment
NODE_ENV=development

# Server
PORT=3002

# Database
# Use :memory: for in-memory database (testing)
# Use ./data for persistent SQLite database
DB_PATH=./data

# Logging
LOG_LEVEL=info
