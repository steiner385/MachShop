import { render } from '@testing-library/react';
import { ValidationIndicator } from './ValidationIndicator';

describe('ValidationIndicator', () => {
  it('renders successfully', () => {
    render(<ValidationIndicator />);
  });
});
